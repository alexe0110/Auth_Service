services:
  admin_panel:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      POSTGRES_HOST: "postgres"
    volumes:
      - static_volume:/opt/app/static
    env_file:
      - ../.env
    depends_on:
      - postgres


volumes:
  static_volume:
    driver: local